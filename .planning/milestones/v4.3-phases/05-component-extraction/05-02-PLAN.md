---
phase: 05-component-extraction
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/page.tsx
  - src/components/explorations/Exploration6.tsx
autonomous: false
requirements: [CLEAN-02]

must_haves:
  truths:
    - "page.tsx renders FloatingNav + gradient background + 6 section components (no Exploration6 import)"
    - "Exploration6.tsx no longer exists"
    - "Gradient animation logic (startGradientAnimation, MutationObserver) lives in page.tsx"
    - "Build succeeds with zero errors"
    - "No e6-* classes remain anywhere in src/"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Page shell with gradient animation + 6 section imports"
      exports: ["default HomePage"]
      min_lines: 50
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/sections/*Section.tsx"
      via: "named imports"
      pattern: "from \"@/components/sections/"
    - from: "src/app/page.tsx"
      to: "@/lib/gsap"
      via: "gsap import for gradient tween"
      pattern: "from \"@/lib/gsap\""
    - from: "src/app/page.tsx"
      to: "src/components/sections/FloatingNav"
      via: "import FloatingNav"
      pattern: "FloatingNav"
---

<objective>
Wire all 6 section components into page.tsx, move gradient animation logic from Exploration6 to page.tsx, delete Exploration6.tsx, and verify the complete extraction.

Purpose: Complete the monolith decomposition -- site renders from independent section components.
Output: Rewritten page.tsx, deleted Exploration6.tsx, green build.
</objective>

<execution_context>
@/Users/khaiphan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khaiphan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-component-extraction/05-RESEARCH.md
@.planning/phases/05-component-extraction/05-01-SUMMARY.md

@src/app/page.tsx
@src/components/explorations/Exploration6.tsx
</context>

<interfaces>
<!-- Section components created by Plan 01 -->
From src/components/sections/HeroSection.tsx:
```typescript
export function HeroSection(): JSX.Element;
```

From src/components/sections/AboutSection.tsx:
```typescript
export function AboutSection(): JSX.Element;
```

From src/components/sections/ExperienceSection.tsx:
```typescript
export function ExperienceSection(): JSX.Element;
```

From src/components/sections/SkillsSection.tsx:
```typescript
export function SkillsSection(): JSX.Element;
```

From src/components/sections/ProjectsSection.tsx:
```typescript
export function ProjectsSection(): JSX.Element;
```

From src/components/sections/ContactSection.tsx:
```typescript
export function ContactSection(): JSX.Element;
```

From src/components/sections/FloatingNav.tsx:
```typescript
export function FloatingNav(): JSX.Element;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite page.tsx and delete Exploration6.tsx</name>
  <files>
    src/app/page.tsx
    src/components/explorations/Exploration6.tsx
  </files>
  <action>
**Step 1: Rewrite page.tsx**

Replace `src/app/page.tsx` entirely. The new page.tsx is a `"use client"` component that:

1. Imports: `useRef, useCallback, useEffect` from react, `gsap` from `@/lib/gsap`, all 6 section components from `@/components/sections/`, `FloatingNav` from `@/components/sections/FloatingNav`.

2. Contains gradient animation logic (extracted from Exploration6.tsx lines 66-122):
   - `gradientTweenRef = useRef<gsap.core.Tween | null>(null)`
   - `startGradientAnimation` useCallback: kill existing tween, detect dark mode, set CSS vars, create gsap.to on documentElement with yoyo repeat -1 duration 25
   - `useEffect`: call startGradientAnimation, set up MutationObserver on documentElement class attribute, cleanup disconnect + kill tween

3. Returns the outer wrapper div with:
   - `className="relative min-h-screen overflow-hidden"`
   - Inline style with gradient background (same as Exploration6.tsx line 288-292)
   - `<FloatingNav />`
   - SVG noise filter (glass-noise)
   - Noise texture overlay div
   - `<HeroSection />`
   - `<AboutSection />`
   - `<ExperienceSection />`
   - `<SkillsSection />`
   - `<ProjectsSection />`
   - `<ContactSection />`

Note: page.tsx does NOT have a containerRef or useGSAP. The gradient animation uses useEffect + gsap.to directly on documentElement (not scoped to a container). This matches the research's Pattern 4.

Use the exact code from the research's "page.tsx After Extraction" code example in 05-RESEARCH.md (lines 253-331) as the reference implementation.

**Step 2: Delete Exploration6.tsx**

Delete `src/components/explorations/Exploration6.tsx`. If the `src/components/explorations/` directory is now empty, delete it too.

**Step 3: Verify build**

Run `npm run build` to confirm no broken imports, no type errors, and the build succeeds.
  </action>
  <verify>
    <automated>test ! -f src/components/explorations/Exploration6.tsx && grep -c "Section" src/app/page.tsx && npm run build</automated>
  </verify>
  <done>
    - page.tsx imports and renders all 6 section components + FloatingNav
    - page.tsx contains gradient animation logic (startGradientAnimation, MutationObserver)
    - page.tsx does NOT import Exploration6
    - Exploration6.tsx is deleted
    - `npm run build` passes with zero errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of extraction fidelity</name>
  <files>none</files>
  <action>
Present the visual verification checklist to the user. The site should function identically to before the extraction -- zero visual or behavioral changes.
  </action>
  <verify>
    <automated>grep -r "e6-" src/ --include="*.tsx" --include="*.css" | wc -l</automated>
  </verify>
  <done>User confirms all animations and interactions work identically to pre-extraction site</done>
  <what-built>Complete extraction of Exploration6.tsx monolith into 6 independent section components. All e6-* classes renamed to BEM-style. Gradient animation moved to page.tsx. Exploration6.tsx deleted.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit http://localhost:3000
    2. Scroll through ALL 6 sections (Hero, About, Experience, Skills, Projects, Contact)
    3. Verify each section's scroll-triggered animations fire correctly:
       - Hero: emoji bounce, glass card fade, name char-by-char reveal with gradient, tagline slide, CTA buttons pop
       - About: heading fade, panels stagger in
       - Experience: heading word-split reveal, cards stagger in
       - Skills: heading word-split reveal, categories stagger in
       - Projects: heading slide from left, cards stagger in
       - Contact: heading word-split reveal, contact cards stagger in
    4. Click "See my work" CTA in Hero -- should smooth-scroll to About section
    5. Click a project card -- should expand to show detail (problem, approach, highlights)
    6. Click same project card again -- should collapse
    7. Toggle dark/light mode -- gradient background should transition smoothly
    8. Verify no visual differences from the pre-extraction site
  </how-to-verify>
  <resume-signal>Type "approved" if all animations and interactions work identically, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- `test ! -f src/components/explorations/Exploration6.tsx` -- monolith deleted
- `ls src/components/sections/{Hero,About,Experience,Skills,Projects,Contact}Section.tsx` -- all 6 exist
- `grep -c "Section" src/app/page.tsx` -- shows 6+ (imports + renders)
- `grep -r "e6-" src/ --include="*.tsx" --include="*.css"` -- zero matches
- `npm run build` -- passes
- Visual checkpoint confirms animation fidelity
</verification>

<success_criteria>
Site renders from 6 independent section components. Exploration6.tsx is deleted. Build passes. All scroll-triggered animations, text reveals, hover interactions, and project card expand/collapse work identically to the pre-extraction site. No e6-* class remnants anywhere in src/.
</success_criteria>

<output>
After completion, create `.planning/phases/05-component-extraction/05-02-SUMMARY.md`
</output>
