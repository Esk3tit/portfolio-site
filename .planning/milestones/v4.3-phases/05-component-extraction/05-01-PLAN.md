---
phase: 05-component-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/NeoBrutalHeading.tsx
  - src/components/sections/HeroSection.tsx
  - src/components/sections/AboutSection.tsx
  - src/components/sections/ExperienceSection.tsx
  - src/components/sections/SkillsSection.tsx
  - src/components/sections/ProjectsSection.tsx
  - src/components/sections/ContactSection.tsx
autonomous: true
requirements: [CLEAN-02]

must_haves:
  truths:
    - "Each section component is a self-contained 'use client' component with its own containerRef and useGSAP hook"
    - "All e6-* class names are replaced with BEM-style {section}-section__{element} names in every section"
    - "NeoBrutalHeading uses split-heading class instead of e6-split-heading"
    - "Each section imports its own data directly from @/data/content"
    - "SplitText instances are tracked and reverted in each section's useGSAP cleanup"
  artifacts:
    - path: "src/components/ui/NeoBrutalHeading.tsx"
      provides: "Renamed split-heading class"
      contains: "split-heading"
    - path: "src/components/sections/HeroSection.tsx"
      provides: "Hero section with char-split SplitText, gradient re-application, scrollToAbout"
      exports: ["HeroSection"]
    - path: "src/components/sections/AboutSection.tsx"
      provides: "About section with heading and panel animations"
      exports: ["AboutSection"]
    - path: "src/components/sections/ExperienceSection.tsx"
      provides: "Experience section with card stagger and word-split heading"
      exports: ["ExperienceSection"]
    - path: "src/components/sections/SkillsSection.tsx"
      provides: "Skills section with category stagger and word-split heading"
      exports: ["SkillsSection"]
    - path: "src/components/sections/ProjectsSection.tsx"
      provides: "Projects section with card expand/collapse state and word-split heading"
      exports: ["ProjectsSection"]
    - path: "src/components/sections/ContactSection.tsx"
      provides: "Contact section with card stagger and word-split heading"
      exports: ["ContactSection"]
  key_links:
    - from: "src/components/sections/*Section.tsx"
      to: "@/lib/gsap"
      via: "import { gsap, ScrollTrigger, useGSAP, SplitText }"
      pattern: "from \"@/lib/gsap\""
    - from: "src/components/sections/*Section.tsx"
      to: "@/data/content"
      via: "named import per section"
      pattern: "from \"@/data/content\""
    - from: "src/components/ui/NeoBrutalHeading.tsx"
      to: "section useGSAP hooks"
      via: "split-heading class queried by containerRef.querySelectorAll"
      pattern: "split-heading"
---

<objective>
Create all 6 independent section components extracted from Exploration6.tsx, each owning its own data imports, animations, and ref scope. Rename the cross-cutting e6-split-heading class first.

Purpose: Break the 787-line monolith into maintainable, independently hot-reloadable section components.
Output: 6 new section component files + updated NeoBrutalHeading.tsx
</objective>

<execution_context>
@/Users/khaiphan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khaiphan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-component-extraction/05-RESEARCH.md

@src/components/explorations/Exploration6.tsx
@src/components/ui/NeoBrutalHeading.tsx
@src/lib/gsap.ts
@src/data/content.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/gsap.ts:
```typescript
export { gsap, ScrollTrigger, useGSAP, SplitText };
```

From src/data/content.ts:
```typescript
export const heroContent = { greeting, name, tagline, ctaText };
export const aboutPanels = [{ emoji, title, content }];
export const experiences = [{ company, title, dates, emoji, bullets }];
export const skillCategories = [{ name, emoji, skills }];
export const projects = [{ title, emoji, description, tags, headerColor, problem, approach, highlights, github?, liveUrl? }];
export const contactLinks = [{ name, emoji, href, label }];
```

From src/components/ui/GlassPanel.tsx:
```typescript
export function GlassPanel({ className, rotate, shadow, tilt, style, children });
```

From src/components/ui/NeoBrutalButton.tsx:
```typescript
export function NeoBrutalButton({ children, onClick, href, download, target, rel, color, rotate, className });
```

From src/components/ui/NeoBrutalHeading.tsx:
```typescript
export function NeoBrutalHeading({ children, emoji, rotate, className });
// Currently has class "e6-split-heading" -- Task 1 renames to "split-heading"
```

From src/components/ui/NeoBrutalTag.tsx:
```typescript
export function NeoBrutalTag({ children });
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rename e6-split-heading and create Hero, About, Experience sections</name>
  <files>
    src/components/ui/NeoBrutalHeading.tsx
    src/components/sections/HeroSection.tsx
    src/components/sections/AboutSection.tsx
    src/components/sections/ExperienceSection.tsx
  </files>
  <action>
**Step 1: Rename NeoBrutalHeading class (do this FIRST -- all new components depend on it)**

In `src/components/ui/NeoBrutalHeading.tsx` line 18, change `e6-split-heading` to `split-heading`. No other changes.

**Step 2: Create HeroSection.tsx**

Create `src/components/sections/HeroSection.tsx` as a `"use client"` component. Extract from Exploration6.tsx lines 316-398 (JSX) and lines 124-197 + 276-281 (animations + scrollToAbout).

Component structure:
- `containerRef = useRef<HTMLDivElement>(null)`
- `scrollToAbout` function using `document.getElementById("about")` -- this intentionally crosses section boundaries
- `useGSAP(() => { ... }, { scope: containerRef })` containing:
  - `splitInstances` array for cleanup
  - Hero timeline with delay 0.15: emoji (scale+rotate back.out), glass (scale+opacity+y), greeting (y+opacity)
  - Hero name char-split: query `.hero-section__name` via containerRef, create SplitText with type "chars", re-apply gradient CSS to `.hero-section__name-gradient div` chars (background, webkitBackgroundClip, webkitTextFillColor, backgroundClip), set initial state gsap.set, animate with heroTl.to
  - Tagline (y+opacity), CTA (scale+rotation+opacity with back.out(2))
  - Double-rAF `ScrollTrigger.refresh(true)` at end
  - Cleanup: revert splitInstances
- NO word-split heading animation (Hero doesn't use NeoBrutalHeading)

BEM class renames for Hero:
- `e6-hero-emoji` -> `hero-section__emoji`
- `e6-hero-glass` -> `hero-section__glass`
- `e6-hero-greeting` -> `hero-section__greeting`
- `e6-hero-name` -> `hero-section__name`
- `e6-hero-name-gradient` -> `hero-section__name-gradient`
- `e6-hero-tagline` -> `hero-section__tagline`
- `e6-hero-cta` -> `hero-section__cta`

Imports: `useRef` from react, `gsap, ScrollTrigger, useGSAP, SplitText` from `@/lib/gsap`, `heroContent` from `@/data/content`, `GlassPanel` from `@/components/ui/GlassPanel`, `NeoBrutalButton` from `@/components/ui/NeoBrutalButton`.

**Step 3: Create AboutSection.tsx**

Create `src/components/sections/AboutSection.tsx` as `"use client"` component. Extract from Exploration6.tsx lines 401-442 (JSX) and lines 222-230 (animations).

Component structure:
- `containerRef = useRef<HTMLDivElement>(null)`
- `useGSAP(() => { ... }, { scope: containerRef })` containing:
  - NO SplitText (About uses manual heading, not NeoBrutalHeading)
  - About heading animation: fromTo `.about-section__heading` (y:40,opacity:0 -> y:0,opacity:1), scrollTrigger trigger `.about-section`, start "top 85%"
  - About panels animation: fromTo `.about-section__panel` (scale:0.95,opacity:0,y:50 -> scale:1,opacity:1,y:0), stagger 0.12
  - Double-rAF ScrollTrigger.refresh(true)
  - No cleanup needed (no SplitText)

BEM class renames for About:
- `e6-about-section` -> `about-section`
- `e6-about-heading` -> `about-section__heading`
- `e6-about-panel` -> `about-section__panel`

Imports: `useRef` from react, `gsap, ScrollTrigger, useGSAP` from `@/lib/gsap`, `aboutPanels` from `@/data/content`, `GlassPanel` from `@/components/ui/GlassPanel`.

**Step 4: Create ExperienceSection.tsx**

Create `src/components/sections/ExperienceSection.tsx` as `"use client"` component. Extract from Exploration6.tsx lines 445-513 (JSX) and lines 233-236 (card animation).

Component structure:
- `containerRef = useRef<HTMLDivElement>(null)`
- `useGSAP(() => { ... }, { scope: containerRef })` containing:
  - `splitInstances` array for cleanup
  - Word-split heading: query `.split-heading` within containerRef, create SplitText type "words", animate words fromTo (y:40,opacity:0 -> y:0,opacity:1, stagger 0.08), scrollTrigger trigger heading start "top 85%"
  - Experience cards: fromTo `.experience-section__card` (y:50,opacity:0,scale:0.95 -> y:0,opacity:1,scale:1), stagger 0.12, scrollTrigger trigger `.experience-section` start "top 85%"
  - Double-rAF ScrollTrigger.refresh(true)
  - Cleanup: revert splitInstances

BEM class renames for Experience:
- `e6-experience-section` -> `experience-section`
- `e6-experience-card` -> `experience-section__card`

Imports: `useRef` from react, `gsap, ScrollTrigger, useGSAP, SplitText` from `@/lib/gsap`, `experiences` from `@/data/content`, `GlassPanel` from `@/components/ui/GlassPanel`, `NeoBrutalHeading` from `@/components/ui/NeoBrutalHeading`.
  </action>
  <verify>
    <automated>ls src/components/sections/HeroSection.tsx src/components/sections/AboutSection.tsx src/components/sections/ExperienceSection.tsx && grep -c "split-heading" src/components/ui/NeoBrutalHeading.tsx && grep -c "e6-" src/components/ui/NeoBrutalHeading.tsx src/components/sections/HeroSection.tsx src/components/sections/AboutSection.tsx src/components/sections/ExperienceSection.tsx</automated>
  </verify>
  <done>
    - NeoBrutalHeading.tsx uses "split-heading" (not "e6-split-heading")
    - HeroSection.tsx exists with hero-section__* classes, char-split SplitText with gradient re-application, scrollToAbout function
    - AboutSection.tsx exists with about-section* classes, heading + panel animations, no SplitText
    - ExperienceSection.tsx exists with experience-section* classes, word-split heading animation, card stagger animation
    - Zero e6-* classes in any of these files
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Skills, Projects, Contact sections</name>
  <files>
    src/components/sections/SkillsSection.tsx
    src/components/sections/ProjectsSection.tsx
    src/components/sections/ContactSection.tsx
  </files>
  <action>
**Step 1: Create SkillsSection.tsx**

Create `src/components/sections/SkillsSection.tsx` as `"use client"` component. Extract from Exploration6.tsx lines 517-552 (JSX) and lines 239-242 (animation).

Component structure:
- `containerRef = useRef<HTMLDivElement>(null)`
- `useGSAP(() => { ... }, { scope: containerRef })` containing:
  - `splitInstances` array for cleanup
  - Word-split heading: query `.split-heading` within containerRef, SplitText type "words", animate (y:40,opacity:0 -> y:0,opacity:1, stagger 0.08), scrollTrigger trigger heading start "top 85%"
  - Skill categories: fromTo `.skills-section__category` (y:40,opacity:0 -> y:0,opacity:1), stagger 0.1, duration 0.7, scrollTrigger trigger `.skills-section` start "top 85%"
  - Double-rAF ScrollTrigger.refresh(true)
  - Cleanup: revert splitInstances

BEM class renames:
- `e6-skills-section` -> `skills-section`
- `e6-skill-category` -> `skills-section__category`

Imports: `useRef` from react, `gsap, ScrollTrigger, useGSAP, SplitText` from `@/lib/gsap`, `skillCategories` from `@/data/content`, `GlassPanel` from `@/components/ui/GlassPanel`, `NeoBrutalHeading` from `@/components/ui/NeoBrutalHeading`, `NeoBrutalTag` from `@/components/ui/NeoBrutalTag`.

**Step 2: Create ProjectsSection.tsx**

Create `src/components/sections/ProjectsSection.tsx` as `"use client"` component. Extract from Exploration6.tsx lines 555-712 (JSX), lines 24-63 (toggleProject + expandedProject state), and lines 245-253 (scroll animations).

Component structure:
- `containerRef = useRef<HTMLDivElement>(null)`
- `const [expandedProject, setExpandedProject] = useState<number | null>(null)` -- local state
- `toggleProject` useCallback (same logic as Exploration6.tsx lines 24-63 but with BEM class names):
  - Query `.projects-section__detail-${index}` via `document.querySelector()` (NOT scoped -- this is an event handler, not useGSAP)
  - Collapse: gsap.to height:0, opacity:0, onComplete setExpandedProject(null)
  - Expand: collapse previous if any, setExpandedProject(index), gsap.fromTo height:0->auto, opacity:0->1
- `useGSAP(() => { ... }, { scope: containerRef })` containing:
  - `splitInstances` array for cleanup
  - Word-split heading: query `.split-heading` within containerRef
  - Projects heading: fromTo `.projects-section__heading` (x:-50,opacity:0 -> x:0,opacity:1), scrollTrigger trigger `.projects-section` start "top 85%"
  - Project cards: fromTo `.projects-section__card` (y:60,opacity:0,scale:0.95 -> y:0,opacity:1,scale:1), stagger 0.12, scrollTrigger trigger `.projects-section` start "top 85%"
  - Double-rAF ScrollTrigger.refresh(true)
  - Cleanup: revert splitInstances

BEM class renames:
- `e6-projects-section` -> `projects-section`
- `e6-projects-heading` -> `projects-section__heading`
- `e6-project-card` -> `projects-section__card`
- `e6-project-detail-${index}` -> `projects-section__detail-${index}`

Imports: `useRef, useState, useCallback` from react, `gsap, ScrollTrigger, useGSAP, SplitText` from `@/lib/gsap`, `projects` from `@/data/content`, `GlassPanel` from `@/components/ui/GlassPanel`, `NeoBrutalButton` from `@/components/ui/NeoBrutalButton`, `NeoBrutalHeading` from `@/components/ui/NeoBrutalHeading`, `NeoBrutalTag` from `@/components/ui/NeoBrutalTag`.

The JSX should include the `expandedProject === index ? "Close" : "View Details"` text in the header bar, and the expandable detail div with class `projects-section__detail-${index}`.

**Step 3: Create ContactSection.tsx**

Create `src/components/sections/ContactSection.tsx` as `"use client"` component. Extract from Exploration6.tsx lines 715-783 (JSX) and lines 256-259 (animation).

Component structure:
- `containerRef = useRef<HTMLDivElement>(null)`
- `useGSAP(() => { ... }, { scope: containerRef })` containing:
  - `splitInstances` array for cleanup
  - Word-split heading: query `.split-heading` within containerRef
  - Contact cards: fromTo `.contact-section__card` (y:40,opacity:0 -> y:0,opacity:1), stagger 0.12, duration 0.7, scrollTrigger trigger `.contact-section` start "top 85%"
  - Double-rAF ScrollTrigger.refresh(true)
  - Cleanup: revert splitInstances

BEM class renames:
- `e6-contact-section` -> `contact-section`
- `e6-contact-card` -> `contact-section__card`

Imports: `useRef` from react, `gsap, ScrollTrigger, useGSAP, SplitText` from `@/lib/gsap`, `contactLinks` from `@/data/content`, `GlassPanel` from `@/components/ui/GlassPanel`, `NeoBrutalButton` from `@/components/ui/NeoBrutalButton`, `NeoBrutalHeading` from `@/components/ui/NeoBrutalHeading`.
  </action>
  <verify>
    <automated>ls src/components/sections/SkillsSection.tsx src/components/sections/ProjectsSection.tsx src/components/sections/ContactSection.tsx && grep -c "e6-" src/components/sections/SkillsSection.tsx src/components/sections/ProjectsSection.tsx src/components/sections/ContactSection.tsx</automated>
  </verify>
  <done>
    - SkillsSection.tsx exists with skills-section* classes, word-split heading, category stagger
    - ProjectsSection.tsx exists with projects-section* classes, local expandedProject state, toggleProject callback, word-split heading, card stagger
    - ContactSection.tsx exists with contact-section* classes, word-split heading, card stagger
    - Zero e6-* classes in any of these files
  </done>
</task>

</tasks>

<verification>
- All 6 section component files exist in src/components/sections/
- NeoBrutalHeading.tsx uses "split-heading" not "e6-split-heading"
- Zero occurrences of "e6-" in any created/modified file
- Each section has: "use client", containerRef, useGSAP with scope, own data import
- HeroSection has char-split SplitText with gradient re-application
- ProjectsSection has local expandedProject state and toggleProject callback
- AboutSection does NOT use SplitText (manual heading)
</verification>

<success_criteria>
All 6 section components created with correct BEM class names, scoped animations, and self-contained data imports. NeoBrutalHeading class renamed. No e6-* remnants in any modified file.
</success_criteria>

<output>
After completion, create `.planning/phases/05-component-extraction/05-01-SUMMARY.md`
</output>
