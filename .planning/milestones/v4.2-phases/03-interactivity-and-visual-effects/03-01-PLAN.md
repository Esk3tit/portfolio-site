---
phase: 03-interactivity-and-visual-effects
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/components/providers/DarkModeProvider.tsx
  - src/components/ui/DarkModeToggle.tsx
  - src/components/ui/GlassPanel.tsx
  - src/components/ui/NeoBrutalTag.tsx
  - src/components/sections/FloatingNav.tsx
  - src/components/explorations/Exploration6.tsx
autonomous: true
requirements:
  - DSGN-03
  - DSGN-04
  - DSGN-05

must_haves:
  truths:
    - "Dark mode toggle switches between light and dark themes with a smooth 300-400ms crossfade"
    - "System preference (prefers-color-scheme) is detected on first load"
    - "Dark mode choice persists in localStorage across page reloads"
    - "No flash of wrong theme (FOUC) on page load"
    - "Glass panels have brighter fill in dark mode and remain visible against dark background"
    - "Subtle noise grain texture is visible on the page background at 5-8% opacity"
    - "Background gradient slowly shifts colors on a 20-30 second cycle"
  artifacts:
    - path: "src/components/providers/DarkModeProvider.tsx"
      provides: "Theme context with isDark state and toggle function"
      exports: ["DarkModeProvider", "useTheme"]
    - path: "src/components/ui/DarkModeToggle.tsx"
      provides: "Sun/moon toggle button UI"
      exports: ["DarkModeToggle"]
    - path: "src/app/globals.css"
      provides: "CSS custom properties for light/dark themes, @custom-variant dark, noise overlay styles"
      contains: "@custom-variant dark"
    - path: "src/app/layout.tsx"
      provides: "Inline script for flash prevention, DarkModeProvider wrapping"
      contains: "dangerouslySetInnerHTML"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/providers/DarkModeProvider.tsx"
      via: "DarkModeProvider wraps children"
      pattern: "<DarkModeProvider>"
    - from: "src/components/sections/FloatingNav.tsx"
      to: "src/components/ui/DarkModeToggle.tsx"
      via: "Toggle rendered inside nav"
      pattern: "<DarkModeToggle"
    - from: "src/components/ui/GlassPanel.tsx"
      to: "src/app/globals.css"
      via: "CSS custom properties var(--glass-fill)"
      pattern: "var\\(--glass"
---

<objective>
Establish dark mode infrastructure with CSS custom properties, flash-prevention script, theme provider, and toggle. Convert GlassPanel and component inline styles to use CSS custom properties so they respond to dark mode. Add page-level noise texture overlay and animated background gradient.

Purpose: This is the visual foundation for Phase 3 -- all subsequent animation work (cursor, micro-interactions, text reveals) needs to function correctly in both light and dark modes. The CSS custom properties set here are consumed by every visual component.

Output: Working dark mode toggle in FloatingNav, smooth theme transitions, noise texture, gradient animation.
</objective>

<execution_context>
@/Users/khaiphan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khaiphan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-interactivity-and-visual-effects/03-CONTEXT.md
@.planning/phases/03-interactivity-and-visual-effects/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/ui/GlassPanel.tsx:
```typescript
interface GlassPanelProps {
  children: ReactNode;
  className?: string;
  as?: "div" | "section" | "article";
  shadow?: string;
  rotate?: string;
  padding?: string;
  style?: CSSProperties;
}
// Currently uses hardcoded rgba values in inline styles -- must convert to CSS custom properties
```

From src/components/sections/FloatingNav.tsx:
```typescript
// "use client" component with sections array, useGSAP, ScrollTrigger
// Renders dots for each section -- DarkModeToggle should be added below the dots
// Uses inline styles with hardcoded colors -- needs CSS custom property conversion for dark mode
```

From src/app/layout.tsx:
```typescript
// Currently: <html> -> <body> -> <SmoothScrollProvider>{children}</SmoothScrollProvider>
// Needs: inline <script> in <head> for flash prevention, DarkModeProvider wrapping children
```

From src/app/globals.css:
```css
/* Currently minimal: @import "tailwindcss", @theme block, body styles */
/* Needs: @custom-variant dark, :root and .dark CSS custom properties, noise overlay, gradient animation */
```

From src/lib/gsap.ts:
```typescript
export { gsap, ScrollTrigger, useGSAP };
// No changes needed for this plan
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dark mode CSS foundation + provider + toggle</name>
  <files>src/app/globals.css, src/app/layout.tsx, src/components/providers/DarkModeProvider.tsx, src/components/ui/DarkModeToggle.tsx, src/components/sections/FloatingNav.tsx</files>
  <action>
1. **globals.css** -- Add Tailwind v4 dark mode and CSS custom properties:
   - Add `@custom-variant dark (&:where(.dark, .dark *));` after @import
   - Add `:root` block with theme-aware custom properties:
     - `--glass-fill: rgba(255, 255, 255, 0.08)`, `--glass-fill-end: rgba(255, 255, 255, 0.03)`
     - `--glass-border: #3d3248`, `--glass-specular-top: rgba(255, 255, 255, 0.7)`
     - `--text-primary: #3d3248`, `--text-body: #5a4d66`, `--text-secondary: #8a7d96`
     - `--bg-gradient-start: #e8ddd5`, `--bg-gradient-end: #cdd0e5`
     - `--accent-purple: #a78bcd`, `--accent-pink: #c9a4b2`, `--accent-blue: #8bb4d4`
   - Add `.dark` block with dark mode values:
     - `--glass-fill: rgba(255, 255, 255, 0.12)`, `--glass-fill-end: rgba(255, 255, 255, 0.06)`
     - `--glass-specular-top: rgba(255, 255, 255, 0.4)`
     - `--text-primary: #e8ddd5`, `--text-body: #c4b8d0`, `--text-secondary: #9a8daa`
     - `--bg-gradient-start: #1a1520`, `--bg-gradient-end: #1e2030`
   - Add smooth theme transition on `html`: `transition: background-color 0.35s ease, color 0.35s ease;`
   - Add `@media (pointer: fine) { * { cursor: none !important; } }` for custom cursor prep (Phase 3 Plan 02 will add the cursor itself)
   - Keep existing @theme block and body styles

2. **DarkModeProvider.tsx** -- Create client component:
   - `"use client"` directive
   - Create ThemeContext with `{ isDark: boolean, toggle: () => void }`
   - useState for isDark, useEffect to sync with `document.documentElement.classList.contains('dark')` on mount
   - toggle function: flip isDark, toggle `.dark` class on html, write to localStorage
   - Export `DarkModeProvider` and `useTheme` hook

3. **layout.tsx** -- Add flash prevention and provider:
   - Add inline `<script>` in `<head>` using dangerouslySetInnerHTML that reads localStorage theme and system preference, adds `.dark` class to html before body renders (see research Pattern 1)
   - Wrap children with `<DarkModeProvider>` inside (but outside of) `<SmoothScrollProvider>`. Order: `<DarkModeProvider><SmoothScrollProvider>{children}</SmoothScrollProvider></DarkModeProvider>`
   - Import DarkModeProvider

4. **DarkModeToggle.tsx** -- Create toggle button:
   - `"use client"` directive
   - Import useTheme from DarkModeProvider
   - Render a button with sun icon (light mode) / moon icon (dark mode) using simple SVG or unicode symbols
   - Style: small circular button matching FloatingNav's dot aesthetic -- use neobrutalist border (2px solid #3d3248), small box-shadow
   - On click: call toggle()
   - Aria-label: "Switch to dark/light mode"

5. **FloatingNav.tsx** -- Add toggle:
   - Import DarkModeToggle
   - Add a thin separator line (1px, rgba) below the section dots
   - Render `<DarkModeToggle />` below the separator
   - Convert any hardcoded colors in inline styles to use CSS custom properties where they would change between themes (dot colors, tooltip background)
  </action>
  <verify>
    <automated>cd /Users/khaiphan/code/portfolio-site && npm run build</automated>
  </verify>
  <done>Dark mode toggles between light (#e8ddd5 gradient) and dark (#1a1520 gradient) themes. No FOUC on reload. Toggle visible in FloatingNav. localStorage persists choice. System preference detected on first visit.</done>
</task>

<task type="auto">
  <name>Task 2: GlassPanel dark mode + noise overlay + gradient animation</name>
  <files>src/components/ui/GlassPanel.tsx, src/components/ui/NeoBrutalTag.tsx, src/components/explorations/Exploration6.tsx</files>
  <action>
1. **GlassPanel.tsx** -- Convert to CSS custom properties:
   - Replace hardcoded `rgba(255,255,255,0.08)` background with `var(--glass-fill)` and `var(--glass-fill-end)` in the linear-gradient
   - Replace hardcoded border color with `var(--glass-border)`
   - Replace specular rim rgba values with `var(--glass-specular-top)` for the top gradient stop; keep bottom stops proportional
   - Add `transition: background 0.35s ease, border-color 0.35s ease;` to the main Tag style for smooth dark mode crossfade
   - Keep all other behavior (shadow, rotate, padding, style spread) unchanged

2. **NeoBrutalTag.tsx** -- Add dark mode support:
   - Replace hardcoded `background: "rgba(255, 255, 255, 0.5)"` with `var(--glass-fill)` or a dedicated `--tag-bg` custom property
   - Replace hardcoded `color: "#3d3248"` with `var(--text-primary)`
   - Add transition for smooth crossfade

3. **Exploration6.tsx** -- Add noise overlay + gradient animation:
   - Add a page-level noise texture overlay div: `pointer-events-none fixed inset-0 z-[1]` with `filter: url(#glass-noise)`, `opacity: 0.06`. This reuses the existing #glass-noise SVG filter already defined in the page.
   - Convert the main background container's gradient to use `var(--bg-gradient-start)` and `var(--bg-gradient-end)` CSS custom properties
   - Add a useGSAP block for gradient animation: animate `--bg-gradient-start` and `--bg-gradient-end` on `document.documentElement` using gsap.to() with `duration: 25`, `ease: "sine.inOut"`, `yoyo: true`, `repeat: -1`. Light targets: startB="#eddcd2", endB="#d5cbe8". The dark mode gradient will automatically use the .dark CSS variable values; the animation should detect dark mode and use appropriate targets (dark startB="#1e1828", dark endB="#222438").
   - Listen for theme changes to restart gradient animation with correct color targets (can use a MutationObserver on html class or re-run on toggle)
   - Convert any other hardcoded color references in the page to use CSS custom properties (text colors especially: #3d3248 -> var(--text-primary), #5a4d66 -> var(--text-body), #8a7d96 -> var(--text-secondary))
   - Add transition properties to the main wrapper for smooth theme crossfade
  </action>
  <verify>
    <automated>cd /Users/khaiphan/code/portfolio-site && npm run build</automated>
  </verify>
  <done>Glass panels show brighter fill in dark mode (rgba 0.12 vs 0.08). Page-level noise grain is subtly visible. Background gradient drifts slowly between warm tones on a ~25s cycle. All components smoothly crossfade between light and dark modes in ~350ms.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no TypeScript errors
2. Page loads without flash of wrong theme when localStorage has dark mode set
3. Toggling dark mode shows smooth crossfade on background, glass panels, text, and all components
4. Noise texture visible on close inspection (6% opacity grain on background)
5. Background gradient shifts slowly over ~25 seconds (watch for color drift)
6. Glass panels are clearly visible against dark background with brighter fill
</verification>

<success_criteria>
- Dark mode fully functional with system preference detection, localStorage persistence, and no FOUC
- All visual components respond to dark mode through CSS custom properties
- Noise overlay adds tactile depth without distracting from content
- Gradient animation is subliminal -- feels alive without being consciously noticeable
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-interactivity-and-visual-effects/03-01-SUMMARY.md`
</output>
