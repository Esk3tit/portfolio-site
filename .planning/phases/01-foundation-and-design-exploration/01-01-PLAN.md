---
phase: 01-foundation-and-design-exploration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/app/explore/[id]/page.tsx
  - src/components/providers/SmoothScroll.tsx
  - src/lib/gsap.ts
autonomous: true
requirements:
  - INFR-06

must_haves:
  truths:
    - "Next.js 16 app runs locally with dev server"
    - "Tailwind v4.2 utility classes render correctly"
    - "GSAP animations execute without errors in the browser"
    - "Lenis smooth scroll is active and synced with GSAP ticker"
    - "Static export builds successfully producing out/ directory"
    - "Routes /explore/1 through /explore/5 exist in build output"
  artifacts:
    - path: "next.config.ts"
      provides: "Static export configuration"
      contains: "output: 'export'"
    - path: "src/lib/gsap.ts"
      provides: "GSAP plugin registration singleton"
      exports: ["gsap", "ScrollTrigger", "useGSAP"]
    - path: "src/components/providers/SmoothScroll.tsx"
      provides: "Lenis + GSAP ticker wiring"
      contains: "gsap.ticker.add"
    - path: "src/app/explore/[id]/page.tsx"
      provides: "Dynamic route for 5 explorations"
      contains: "generateStaticParams"
    - path: "src/app/globals.css"
      provides: "Tailwind v4 @theme tokens"
      contains: "@theme"
  key_links:
    - from: "src/components/providers/SmoothScroll.tsx"
      to: "src/lib/gsap.ts"
      via: "import gsap for ticker sync"
      pattern: "import.*gsap.*from.*@/lib/gsap"
    - from: "src/app/layout.tsx"
      to: "src/components/providers/SmoothScroll.tsx"
      via: "SmoothScrollProvider wraps children"
      pattern: "SmoothScrollProvider"
    - from: "src/app/explore/[id]/page.tsx"
      to: "src/components/explorations/"
      via: "dynamic component loading by id"
      pattern: "Exploration[1-5]"
---

<objective>
Scaffold the complete Next.js 16 app shell with Tailwind v4.2, GSAP 3.14, and Lenis configured and working. Create the routing infrastructure for 5 design explorations as dynamic routes with static export compatibility. This is the foundation every subsequent plan builds on.

Purpose: Establish a working, buildable app shell so design exploration plans can focus purely on creative work without infrastructure concerns.
Output: Running Next.js app with dev server, static export, smooth scroll, GSAP animations, and 5 placeholder exploration routes.
</objective>

<execution_context>
@/Users/khaiphan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khaiphan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-design-exploration/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 project and configure static export with GSAP + Lenis</name>
  <files>next.config.ts, package.json, src/app/layout.tsx, src/app/globals.css, src/lib/gsap.ts, src/components/providers/SmoothScroll.tsx</files>
  <action>
1. Scaffold the project using `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --yes` (the `.` installs into the current directory since the repo already exists). If it complains about existing files, use a temp directory and move files.

2. Install animation dependencies: `npm install gsap @gsap/react lenis`

3. Configure `next.config.ts` for static export:
```typescript
import type { NextConfig } from 'next'
const nextConfig: NextConfig = {
  output: 'export',
  images: { unoptimized: true },
}
export default nextConfig
```

4. Create `src/lib/gsap.ts` — single GSAP plugin registration point:
- "use client" directive
- Import gsap, ScrollTrigger from gsap, useGSAP from @gsap/react
- Register plugins with gsap.registerPlugin(ScrollTrigger, useGSAP)
- Re-export gsap, ScrollTrigger, useGSAP

5. Create `src/components/providers/SmoothScroll.tsx` — Lenis + GSAP ticker sync:
- "use client" directive
- Use ReactLenis from "lenis/react" with `autoRaf: false`
- Wire lenis.raf into gsap.ticker.add callback (multiply time by 1000)
- Set gsap.ticker.lagSmoothing(0)
- Cleanup: gsap.ticker.remove on unmount
- Follow the exact pattern from 01-RESEARCH.md Pattern 3

6. Update `src/app/globals.css` with Tailwind v4 @theme tokens:
- `@import "tailwindcss";`
- Define `@theme` block with: --font-display (for headings), --font-body (for text), base color tokens (--color-surface, --color-text, --color-primary-500), --ease-smooth cubic-bezier
- These are baseline tokens; each exploration overrides them locally

7. Update `src/app/layout.tsx`:
- Import Inter and Space_Grotesk from next/font/google with CSS variable names --font-body and --font-display
- Apply font variables to html element className
- Wrap children in SmoothScrollProvider
- Set metadata: title "Khai Phan - Design Explorations", description "Portfolio design direction preview"
- Body class: font-body antialiased
  </action>
  <verify>
    <automated>cd /Users/khaiphan/code/portfolio-site && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Project scaffolded, dev server starts without errors, `npm run build` succeeds, GSAP and Lenis packages installed</done>
</task>

<task type="auto">
  <name>Task 2: Create dynamic exploration routes with placeholder components and index page</name>
  <files>src/app/explore/[id]/page.tsx, src/app/page.tsx, src/components/explorations/Exploration1.tsx, src/components/explorations/Exploration2.tsx, src/components/explorations/Exploration3.tsx, src/components/explorations/Exploration4.tsx, src/components/explorations/Exploration5.tsx</files>
  <action>
1. Create `src/app/explore/[id]/page.tsx`:
- Export `generateStaticParams` returning ids 1-5 for static export compatibility
- Accept `params: Promise<{ id: string }>` and await it (Next.js 16 async params)
- Use a mapping object or switch to render the correct Exploration component by id
- Import all 5 exploration components statically (they are client components, tree-shaking handles unused ones per route)
- Show a "Design not found" fallback for invalid ids

2. Create 5 placeholder exploration components in `src/components/explorations/`:
- Each file: "use client", imports useRef from react, imports gsap + useGSAP from @/lib/gsap
- Each renders a full-viewport Hero section with:
  - The exploration name as h1 (e.g., "Light + Airy")
  - A short description paragraph
  - A simple GSAP entrance animation (fade + slide up on the h1) using useGSAP with scope
- Use basic Tailwind classes for layout (min-h-screen, flex, items-center, etc.)
- These are PLACEHOLDERS — Plans 02 and 03 will replace the content with full creative designs
- Each component must be a valid, rendering client component that proves GSAP works

3. Create `src/app/page.tsx` — exploration index:
- Display "Design Explorations" heading
- List all 5 explorations as Link cards with name and description
- Use Tailwind for a simple grid layout (responsive: 1 col mobile, 2-3 cols desktop)
- Each card links to /explore/{id}
- Include brief instructions: "Browse each direction, then pick your favorite"

4. Verify all 5 routes build: `npm run build` should show /explore/1 through /explore/5 in output
  </action>
  <verify>
    <automated>cd /Users/khaiphan/code/portfolio-site && npm run build 2>&1 | grep -E "explore/[1-5]" | wc -l</automated>
  </verify>
  <done>All 5 exploration routes render in dev server, `npm run build` produces out/explore/1 through out/explore/5, index page links to all 5, each placeholder shows a GSAP-animated heading</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors and produces `out/` directory
- `ls out/explore/` shows directories 1 through 5
- `npm run dev` starts without errors
- Visiting localhost:3000 shows the exploration index with 5 links
- Visiting localhost:3000/explore/1 through /explore/5 shows placeholder content with animated headings
- Smooth scroll (Lenis) is active on all pages
</verification>

<success_criteria>
- Next.js 16 dev server runs without errors
- Static export build succeeds with all 5 exploration routes
- GSAP animations execute (heading fade-in visible on exploration pages)
- Lenis smooth scroll is active (scroll feels smooth, not native)
- Tailwind utility classes render correctly
- All packages at specified versions (Next.js 16.x, Tailwind 4.2.x, GSAP 3.14.x, Lenis 1.3.x)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-design-exploration/01-01-SUMMARY.md`
</output>
