---
phase: 04-code-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/globals.css
autonomous: true
requirements:
  - CLEAN-05

must_haves:
  truths:
    - "globals.css contains no CSS classes that are unreferenced in any TSX file"
    - "No unused CSS custom properties remain in globals.css"
    - "No unused TypeScript imports or exports exist across the codebase"
    - "next build completes with zero warnings from removed files"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Cleaned CSS with only production styles"
      contains: "@theme"
  key_links:
    - from: "src/app/globals.css"
      to: "src/components/explorations/Exploration6.tsx"
      via: "CSS custom properties consumed as var(--name)"
      pattern: "var\\(--"
---

<objective>
Remove all dead CSS and TypeScript code, then verify the entire cleanup with a clean build.

Purpose: Ensure zero dead code remains after the file deletions in Plan 01 -- the codebase should contain only actively-used production code.
Output: Clean globals.css, zero unused imports, passing build.
</objective>

<execution_context>
@/Users/khaiphan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khaiphan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-code-cleanup/04-RESEARCH.md
@.planning/phases/04-code-cleanup/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and remove dead CSS and TypeScript code</name>
  <files>src/app/globals.css</files>
  <action>
    **CSS cleanup:**
    1. Read `src/app/globals.css` in full.
    2. Remove the `.e6-link-underline` class and its `@media (hover: hover)` hover rule (confirmed dead -- zero references in any TSX file). These are approximately lines 58-72.
    3. For every remaining CSS class name in globals.css (if any beyond e6-link-underline), grep all TSX files to confirm it is referenced. Remove any unreferenced classes.
    4. For every CSS custom property (--var-name) defined in `:root` or `.dark`, grep all files (TSX, CSS) for usage via `var(--var-name)`. Do NOT remove any that are referenced -- research confirmed all current custom properties are actively used by Exploration6 and UI components. Only remove if grep confirms zero references.

    **TypeScript dead code sweep:**
    5. After Plan 01 deleted 12 files, check for any newly-orphaned code:
       - Grep for any remaining imports of `Exploration1` through `Exploration5` in any file
       - Grep for any imports of deleted files (explore route files)
       - Check each file in `src/components/` and `src/lib/` for unused exports (exports not imported by any other file)
    6. Remove any dead imports or orphaned exports found.

    IMPORTANT: Do NOT remove CSS custom properties without grep verification. Properties like `--glass-fill`, `--accent-pink`, `--accent-blue` are consumed via `var()` in component inline styles and Tailwind classes. When in doubt, keep it.
  </action>
  <verify>
    <automated>grep -r "e6-link-underline" src/ && echo "FAIL: e6-link-underline still referenced" || echo "PASS: e6-link-underline removed"</automated>
  </verify>
  <done>
    - e6-link-underline class and its hover rule removed from globals.css
    - No CSS classes in globals.css are unreferenced in the codebase
    - No unused TypeScript imports or exports remain
    - All actively-used CSS custom properties preserved
  </done>
</task>

<task type="auto">
  <name>Task 2: Build verification and final check</name>
  <files>
    src/app/globals.css
    src/app/layout.tsx
    src/app/page.tsx
    src/components/explorations/Exploration6.tsx
  </files>
  <action>
    1. Run `npx next build` -- must complete successfully with zero errors.
    2. Check build output for any warnings about unused imports or missing modules related to deleted files.
    3. If any build errors occur:
       - Read the error message carefully
       - Fix the root cause (likely a lingering import of a deleted file)
       - Re-run `npx next build` until it passes clean
    4. Final file inventory -- confirm only production files remain:
       - `ls src/app/` should show: globals.css, favicon.ico, icon1.png, icon0.svg, apple-icon.png, manifest.json, layout.tsx, page.tsx (no explore/)
       - `ls src/components/explorations/` should show only Exploration6.tsx
       - `ls public/` should show: logo.png, habbycursor*.png (3 files), resume.pdf, web-app-manifest-192x192.png, web-app-manifest-512x512.png
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - next build completes with exit code 0
    - No warnings about missing imports or unused files in build output
    - File inventory matches expected production-only state
  </done>
</task>

</tasks>

<verification>
- `grep -r "e6-link-underline" src/` returns no results
- `grep -r "Exploration[1-5]" src/` returns no results
- `npx next build` exits 0 with no warnings from deleted files
- globals.css contains only actively-used styles
</verification>

<success_criteria>
- Dead CSS removed (e6-link-underline gone, all remaining classes verified as used)
- Dead TypeScript removed (no orphaned imports/exports)
- Build passes clean -- zero errors, zero warnings from cleanup
- Codebase contains only production code
</success_criteria>

<output>
After completion, create `.planning/phases/04-code-cleanup/04-02-SUMMARY.md`
</output>
