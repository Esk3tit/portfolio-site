---
phase: 06-responsive-design
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/HeroSection.tsx
  - src/components/sections/AboutSection.tsx
  - src/components/sections/ExperienceSection.tsx
  - src/components/sections/SkillsSection.tsx
  - src/components/sections/ProjectsSection.tsx
  - src/components/sections/ContactSection.tsx
  - src/components/ui/NeoBrutalHeading.tsx
  - src/components/ui/NeoBrutalButton.tsx
  - src/components/ui/NeoBrutalTag.tsx
autonomous: true
requirements:
  - RESP-01
  - RESP-04

must_haves:
  truths:
    - "On a 375px screen, all content is readable without horizontal scrolling"
    - "Grids collapse to single column on mobile"
    - "Hero text scales down to text-3xl on mobile, subtitle to text-sm"
    - "Section vertical padding reduces to py-16 on mobile"
    - "All buttons and interactive elements have at least 44px touch targets on mobile"
    - "Section headings scale to text-xl on mobile"
  artifacts:
    - path: "src/components/ui/NeoBrutalHeading.tsx"
      provides: "Responsive heading with text-xl on mobile, text-2xl on sm, text-3xl on md"
      contains: "text-xl"
    - path: "src/components/ui/NeoBrutalButton.tsx"
      provides: "Button with min-h-[44px] on mobile"
      contains: "min-h-"
    - path: "src/components/sections/HeroSection.tsx"
      provides: "Hero with text-3xl mobile, reduced glass panel padding"
      contains: "text-3xl"
  key_links:
    - from: "src/components/sections/HeroSection.tsx"
      to: "Tailwind responsive classes"
      via: "text-3xl sm:text-6xl md:text-7xl breakpoint chain"
      pattern: "text-3xl.*sm:text-6xl.*md:text-7xl"
    - from: "src/components/ui/NeoBrutalButton.tsx"
      to: "Touch target sizing"
      via: "min-h-[44px] sm:min-h-0 responsive class"
      pattern: "min-h-\\[44px\\]"
---

<objective>
Make all existing sections and UI components responsive for mobile (375px+) and tablet (640-1024px) with proper typography scaling, padding, grid collapse, and 44px touch targets.

Purpose: The site content currently overflows or looks cramped on mobile. This plan applies responsive Tailwind classes to every section and enforces touch-friendly sizing on all interactive elements.
Output: All 6 sections and 3 UI components updated with responsive classes.
</objective>

<execution_context>
@/Users/khaiphan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khaiphan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-responsive-design/06-CONTEXT.md
@.planning/phases/06-responsive-design/06-RESEARCH.md

@src/components/sections/HeroSection.tsx
@src/components/sections/AboutSection.tsx
@src/components/sections/ExperienceSection.tsx
@src/components/sections/SkillsSection.tsx
@src/components/sections/ProjectsSection.tsx
@src/components/sections/ContactSection.tsx
@src/components/ui/NeoBrutalHeading.tsx
@src/components/ui/NeoBrutalButton.tsx
@src/components/ui/NeoBrutalTag.tsx

<interfaces>
<!-- Current class patterns to update (from codebase) -->

From src/components/ui/NeoBrutalHeading.tsx (line 18):
```typescript
className={`split-heading inline-block px-6 py-3 text-2xl font-bold md:text-3xl ${className}`}
// Target: text-xl sm:text-2xl md:text-3xl, px-4 py-2.5 sm:px-6 sm:py-3
```

From src/components/ui/NeoBrutalButton.tsx (line 28):
```typescript
const baseClassName = `rounded-xl px-8 py-3.5 text-sm font-bold uppercase tracking-wider ${className}`;
// Target: add min-h-[44px] sm:min-h-0
```

From src/components/ui/NeoBrutalTag.tsx:
```typescript
// Review for touch target -- needs min-h-[44px] on mobile if interactive
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update UI components with responsive sizing</name>
  <files>src/components/ui/NeoBrutalHeading.tsx, src/components/ui/NeoBrutalButton.tsx, src/components/ui/NeoBrutalTag.tsx</files>
  <action>
**NeoBrutalHeading.tsx** -- Update className on the h2 element:
- Before: `px-6 py-3 text-2xl font-bold md:text-3xl`
- After: `px-4 py-2.5 text-xl font-bold sm:px-6 sm:py-3 sm:text-2xl md:text-3xl`
- This scales heading to text-xl on mobile (<640px), text-2xl on sm (640px+), text-3xl on md (768px+)
- Padding also reduces on mobile for visual proportion

**NeoBrutalButton.tsx** -- Update baseClassName:
- Before: `rounded-xl px-8 py-3.5 text-sm font-bold uppercase tracking-wider`
- After: `rounded-xl px-6 py-3.5 min-h-[44px] sm:min-h-0 sm:px-8 text-sm font-bold uppercase tracking-wider`
- `min-h-[44px]` enforces 44px touch target on mobile
- `sm:min-h-0` removes the constraint on desktop (where hover provides feedback)
- `px-6 sm:px-8` slightly reduces horizontal padding on mobile for narrower screens

**NeoBrutalTag.tsx** -- Review and update if interactive:
- If tags are purely decorative (no onClick), no touch target change needed
- If tags have any interactive behavior, add `min-h-[44px] sm:min-h-0` to their container
- Reduce horizontal padding on mobile if needed: `px-3 sm:px-4`
- Check current implementation first -- tags in Skills/Projects sections may just be display elements
  </action>
  <verify>
    <automated>cd /Users/khaiphan/code/portfolio-site && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>NeoBrutalHeading shows text-xl on mobile with smaller padding. NeoBrutalButton has 44px min touch target on mobile. NeoBrutalTag reviewed and updated if interactive. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Apply responsive classes to all section components</name>
  <files>src/components/sections/HeroSection.tsx, src/components/sections/AboutSection.tsx, src/components/sections/ExperienceSection.tsx, src/components/sections/SkillsSection.tsx, src/components/sections/ProjectsSection.tsx, src/components/sections/ContactSection.tsx</files>
  <action>
Apply these responsive changes per user decisions in CONTEXT.md. Read each file first, then apply the specific changes:

**All sections -- vertical padding:**
- Find each section's root `<section>` element
- Change `py-28` to `py-16 sm:py-28` (reduced padding on mobile)
- Keep existing horizontal padding (`px-6 md:px-12`) as-is

**HeroSection.tsx:**
- Hero name h1: change `text-5xl` to `text-3xl` (keep `sm:text-6xl md:text-7xl`)
- Hero subtitle: add `text-sm sm:text-base` (or adjust existing -- drops to text-sm on mobile)
- Hero glass panel: change padding to `px-6 py-10 sm:px-10 sm:py-14 md:px-16 md:py-18` (reduced on mobile, full-width feel with edge-to-edge minus page padding)
- Check hero section min-height works on small screens -- may need `min-h-[80vh] sm:min-h-screen` if content overflows

**AboutSection.tsx:**
- Section padding: `py-16 sm:py-28`
- Check text sizing -- body text should remain readable at default size
- Any internal grid or flex layout: verify it stacks on mobile

**ExperienceSection.tsx:**
- Section padding: `py-16 sm:py-28`
- Experience cards: reduce internal padding on mobile: `px-5 py-6 sm:px-8 sm:py-8`
- Cards should stack vertically full-width on mobile (check if already single-column)
- All details visible on mobile -- no hide/reveal per user decision

**SkillsSection.tsx:**
- Section padding: `py-16 sm:py-28`
- Skills grid: should already be single column on mobile via `md:grid-cols-2` -- verify this
- If skill tags are small, verify readability at mobile size

**ProjectsSection.tsx:**
- Section padding: `py-16 sm:py-28`
- Project cards: keep inline expand behavior on all devices (no modal) per user decision
- Reduce card padding on mobile if needed
- Project expand buttons: ensure 44px touch target via padding or min-height
- Check any grid layouts collapse properly

**ContactSection.tsx:**
- Section padding: `py-16 sm:py-28`
- Contact links: should already stack vertically on mobile via `sm:flex-row` -- verify
- Contact link cards: ensure 44px touch targets
- Reduce internal padding if needed for narrow screens

General approach for each file:
1. Read the file
2. Identify the root section element and update padding
3. Find typography classes and adjust for mobile
4. Find grid/flex layouts and verify mobile collapse
5. Check interactive elements for 44px touch targets
6. Ensure no horizontal overflow at 375px (watch for fixed widths, large padding, or wide content)
  </action>
  <verify>
    <automated>cd /Users/khaiphan/code/portfolio-site && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>All 6 sections have responsive padding (py-16 on mobile), hero text scales to text-3xl on mobile, grids collapse to single column, experience cards are full-width on mobile, and all interactive elements meet 44px touch targets. No horizontal scroll at 375px. Build passes.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- At 375px viewport: no horizontal scrollbar, all text readable
- Hero name appears at ~30px (text-3xl), subtitle at text-sm
- Section headings appear at text-xl on mobile
- Section padding visibly reduced on mobile vs desktop
- Skills grid is single column on mobile
- Experience cards stack vertically, full-width
- All buttons have at least 44px height on mobile
- At 1024px+: layout identical to pre-change desktop appearance
</verification>

<success_criteria>
- All 6 sections have responsive padding (py-16 mobile, py-28 desktop)
- Hero text: text-3xl mobile, text-6xl sm, text-7xl md
- NeoBrutalHeading: text-xl mobile, text-2xl sm, text-3xl md
- NeoBrutalButton: min-h-[44px] on mobile
- No horizontal scroll at 375px
- Desktop layout unchanged
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/06-responsive-design/06-02-SUMMARY.md`
</output>
